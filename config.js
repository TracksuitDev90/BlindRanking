// Copy this to config.js (same folder) and add your TMDB v3 API key.
// For basic read-only usage, client-side is fine. For stricter limits/hiding keys,
// proxy TMDB requests via a serverless function later.
window.BR_CONFIG = {
  TMDB_API_KEY: "REPLACE_WITH_YOUR_TMDB_V3_KEY"
};